# Copy this file to .env (or .env.txt on Windows) and fill in your own keys.
# This file (.env.example) is safe to commit; the actual .env file is gitignored.

# Etherscan API V2 — free tier sufficient
# Sign up: https://etherscan.io/myapikey
ETHERSCAN_API_KEY=YOUR_KEY_HERE
